# await message.answer(f'Привіт, {message.from_user.fullname}')
# awwait message.reply

# @router.message(Command(commands=['info']))

# @router.message()
# async def echo(message: Message):
#     await message.answer(message.text)